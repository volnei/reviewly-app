# reviewly-app

Landing page for **Reviewly** — the desktop PR review console.

Live: https://volnei.github.io/reviewly-app/

Static single-page site (vanilla HTML/CSS/JS, no build step), served via GitHub Pages.
The app itself lives in a separate repository.
